<!DOCTYPE html>
<html>
<head>
  <title>Engines</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => false %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href=<%= root_path %> ><i class="fa fa-gears"></i> Control Panel</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if params[:controller] == 'pages' && params[:action] == 'home' %>">
          <%= link_to content_tag(:i, "", class: "fa fa-times") + " Close", root_path %>
        </li>
        <li class="<%= 'active' if params[:controller] == 'pages' && params[:action] == 'app_manager' %>">
          <%= link_to content_tag(:i, "", class: "fa fa-play") + " App Manager", app_manager_path %>
        </li>
        <li class="<%= 'active' if params[:controller] == 'pages' && params[:action] == 'install' %>">
          <%= link_to content_tag(:i, "", class: "fa fa-plus-square") + " Installer", installer_path %>
        </li>
        <li class="<%= 'active' if params[:controller] == 'pages' && params[:action] == 'install' %>">
          <%= link_to content_tag(:i, "", class: "fa fa-gear") + " Settings", settings_path %>
        </li>
        <li class="<%= 'active' if params[:controller] == 'pages' && params[:action] == 'install' %>">
          <%= link_to content_tag(:i, "", class: "fa fa-dashboard") + " System", system_path %>
        </li>
        <li class="<%= 'active' if params[:controller] == 'pages' && params[:action] == 'install' %>">
          <%= link_to content_tag(:i, "", class: "fa fa-users") + " Users", users_path %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if params[:controller] == 'pages' && params[:action] == 'help' %>">
          <%= link_to content_tag(:i, "", class: "fa fa-question") + " Help", help_path %>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= content_tag(:i, "", class: "fa fa-user") %> <%= current_user.email %><span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to content_tag(:i, "", class: "fa fa-sign-out") + " Sign out", destroy_user_session_path, :method => :delete %></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div id="content">
  <div class="container">
    <div class="row">
      <div class="navbar-buffer"></div>      
      <div class="col-sm-12">
        <% flash.each do |name, msg| %>
          <% if msg.is_a?(String) %>
            <div class="alert alert-<%= name.to_s == 'notice' ? 'success' : 'danger' %>">
              <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
              <%= content_tag :div, msg, :id => "flash_#{name}" %>
            </div>
          <% end %>
        <% end %>
      </div>
      <%= yield %>
    </div>      
  </div>
</div>

<div id="footer">
</div>

</body>
</html>
