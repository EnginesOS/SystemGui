<% f.object.connection_params.each do |key,value| %>
<%= hidden_field_tag "application_service[connection_params][#{key}]",value %>
<% end %>
<%= render partial: 'shared/form/errors', locals: {f: f} %>
<%= render partial: 'variables/form_fields', locals: {f: f} %>
