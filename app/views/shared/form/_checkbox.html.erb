<%  label ||= name.to_s.humanize.capitalize
    tooltip ||= 'undefined' %>

<div class="col-sm-8 col-sm-offset-2">
  <div class="form-group">
    <%= f.check_box(name) + ' ' + f.label(name, label, class: 'label-lg') %>
  </div>
</div>