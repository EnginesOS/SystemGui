<div class="fields">
  <div class="form-group form-group-lg">
    <div class="col-sm-2">
    </div>
    <div class="col-sm-10">
      <%= eval "f.label :#{field[:name]}, class: 'btn btn-default btn-lg' do; (f.check_box :#{field[:name]}) + ' #{(field[:label] || field[:name].to_s.humanize.capitalize)}'; end" %>
    </div>
  </div>
</div>
