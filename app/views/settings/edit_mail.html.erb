<div class="col-sm-12">
  <h2><i class="fa fa-envelope-o"></i> Mail</h2>
  <%= form_for @settings do |f| %>
    <%= render partial: 'shared/form/errors', locals: {f: f} %>
    <%= render partial: 'shared/form/text_field', locals: {name: :smtp_smarthost, label: "SMTP Smarthost", f: f} %>
    <%= render partial: 'shared/form/cancel_and_submit_buttons', locals: {f: f, cancel_path: control_panel_path} %>
  <% end %>
</div>